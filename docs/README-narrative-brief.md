# README 叙事口径大纲（给 Claude 协作的输入）

> **用途**：这份大纲交给负责代码/项目的 Claude，确保 GitHub README 与初赛《问题定义文档》叙事一致。
>
> **为什么重要**：评委看完 4 页问题定义文档后，第一件事是点进 GitHub。如果 README 还在讲"红队测试工具"，而文档讲的是"对齐盲区探索环境"——叙事割裂，评委会质疑项目定位不清。
>
> **原则：工具是载体，发现是卖点。仓库还是那个仓库，代码还是那些代码，但开篇的"它是什么"必须改。**

---

## 一句话定位（README 顶部 hero 必须用这个）

**❌ 不要写**：
> agent-redteam 是一个开源的 LLM 红队测试工具 / 安全扫描器 / benchmark。

**✅ 要写**：
> **agent-redteam 是一个面向 LLM 安全对齐「跨通道盲区」的 Agent 探索环境。**
> 它用一套固定的样本集和评分器，让 Agent 在五个攻击通道（文本/变异/多轮/多模态/进化）上系统探索，产出可复现的对齐规律发现，而非零散的漏洞列表。

---

## README 结构（建议 7 段，按此顺序）

### 1. Hero（最关键）
- 一句话定位（上面的 ✅ 版本）
- 一行最 punchy 的发现作证据（校正后口径，不要再用 73%/26.7）：`同一模型：纯文本 bypass ~48% → 文档管道 ~1%（N=120）；真视觉通道跨 6 模型 0.8%–46.7%，强能力依赖——不是普遍盲区`
- 安装 + 一行复现命令（评委 30 秒内要能跑起来）

### 2. 为什么存在（Motivation）——不是"功能列表"
讲清楚要回答的**研究问题**：
> 当前 LLM 对齐是否存在随攻击通道系统性迁移的盲区？
- 引用 OWASP LLM01 / GPT-Red / arXiv:2509.05883 证明问题真实
- 强调"现有 benchmark 各自只覆盖单一通道，无人做跨通道对照"

### 3. 它是什么：探索环境（不是工具）
用**固定 / 可探索 / 反馈**三段式介绍（与问题定义文档第 3 节完全一致）：
- **固定**：14 套件、2319 样本、评分器、随机种子
- **可探索**：5 个通道维度的表格
- **反馈**：结构化反馈支持 Agent 迭代（D5 进化）

### 4. 已发现的信号（Findings）——README 的灵魂
把论文的结果表搬上来，但**每个数字配一句"它意味着什么"**：
| 发现 | 数据 | 意味着 |
|---|---|---|
| 评分器自我校正 | 初测 73% → 校正 47%（7/15） | 旧数字被 length 兜底假阳性夸大，诚实校正抗审 |
| 文档管道 vs 纯文本（负结果） | 47.1%→0.8%、49.2%→1.7%（N=120） | 非文本通道反而更难攻破，推翻"隐藏=更危险" |
| 真视觉通道能力依赖 | 跨 6 模型 0.8%–46.7% | 不是普遍盲区，强弱模型差异 >45pp |
| A/B/C 感知拆解（方法学） | minimax 1.7% bypass 但 A=103/120 | 低 bypass 可能是"瞎"不是"安全" |
| 分层注入负结果 | H3 被证伪，显眼度主导 | 隐藏深度不放大攻击 |

### 5. 快速开始（Quick Start）
- `pip install agent-redteam`
- `agent-redteam scan --target zai --model GLM-5.2 --limit 30`
- **重要**：这条命令必须真能跑通、真能出可对照日志（复赛门槛）

### 6. 复现与可检查性
- 随机种子说明
- 日志格式（评委要看 Agent 怎么行动）
- validation/ 目录的数据说明

### 7. 贡献与社区（可延续性）
- 新通道、新变异策略如何作为"可探索部分"被贡献
- 发现信号定义保持稳定（这是"环境包"可被接续的关键）

---

## 三个绝对不能出现的东西（会害你掉档）

1. **"又一个 benchmark"的自我定位**——会让你掉进 HarmBench/AdvBench 的红海，被有算力的团队碾压。
2. **"100% bypass rate" 当卖点**——D5 的 100% 是 n=2，统计不充分。README 里要么不提，要么老实写"Round 1 initial signal, needs larger-scale validation"。
3. **"8 model targets" 与实际只测 GLM 的不符**——这条警告已过时（2026-08-09）：现已实际测 8 个模型（2 文本 GLM-5.2/DeepSeek-v4 + 6 视觉 doubao-pro/code/lite、glm-4v-flash、kimi-k2-7、minimax-m3），另有 GLM-4.5/4-Flash 作早期基线。README 可以如实宣传"已测 8 模型"。

---

## 与问题定义文档的术语对照（必须统一）

| 问题定义文档用 | README 必须沿用 | 不要混用 |
|---|---|---|
| 探索环境（exploration environment） | 探索环境 | ❌ 测试工具/扫描器 |
| 通道（channel）/ 维度（dimension） | 通道/维度 | ❌ 攻击类型（可偶尔用，但主体用通道） |
| 发现信号（discovery signal） | 发现信号 | ❌ 结果/分数 |
| 固定/可探索/反馈 | 固定/可探索/反馈 | — |
| 跨通道盲区 | 跨通道盲区 | ❌ 安全漏洞 |

---

**这份大纲交给 Claude 时附一句话**：
> "README 的叙事口径必须和 `docs/goai-problem-definition.pdf` 一致——我们参加 GOAI 赛道三开放探索赛题，定位是'对齐盲区研究'，不是'红队工具'。代码功能不动，但 README 的开篇叙事按这份大纲改。"
